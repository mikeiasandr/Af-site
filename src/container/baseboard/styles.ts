import styled from "styled-components";

export const Container = styled.main`
    padding: 2rem 5rem;
    h4{ 
        color: var(--font);
        text-align: center;
        align-items: center;
        margin: auto;
    }
    
`;